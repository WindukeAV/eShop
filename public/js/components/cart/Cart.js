function NavbarCartView(containerNode) {
  View.call(this, containerNode);
}

function NavbarCartController(containerNode) {
  
}







function CartView(containerNode) {
  View.call(this, containerNode);

  // TODO: in progress
}